# Ingestion / parsing
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pandas==2.2.3
tqdm==4.66.5

# Config
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1

# DB
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
pgvector==0.3.6

# Embedding / Generation (HuggingFace)
# torch/transformers/accelerate는 FlagEmbedding 설치 시 의존성 해석으로
# 실제 설치본이 더 최신 버전으로 올라갔다(pip freeze 기준 반영).
torch==2.13.0
transformers==5.14.1
FlagEmbedding==1.4.0
accelerate==1.14.0
bitsandbytes==0.50.0

# API / UI
fastapi==0.141.1
uvicorn[standard]==0.52.1
streamlit==1.61.1

# Evaluation
ragas==0.4.3
langchain-huggingface==1.2.2
rank_bm25==0.2.2

# Testing
pytest==8.3.3
